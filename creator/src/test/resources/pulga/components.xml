<?xml version="1.0" encoding="ISO-8859-1"?>

<root>
	<!-- same key used on pulga.xml -->
	<configuration key="uses-simple">
		<some key="value"/>
	</configuration>
	
	<configuration key="xingu.factory.test.IsConfigurable">
		<some key="value2"/>
	</configuration>
	
	<!-- this configuration object should not be overridden by it's iline counterpart -->
	<configuration key="xingu.factory.test.IsConfigurable2">
		<some key="should not read this value"/>
	</configuration>
	
</root>